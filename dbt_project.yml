name: 'sovereign_debt_observatory'
version: '1.0.0'
config-version: 2

profile: 'sovereign_debt_observatory'

model-paths: ["models"]


clean-targets:
  - "target"
  - "dbt_packages"

models:
  sovereign_debt_observatory:
    staging:
      +materialized: view
      +schema: staging
    mart:
      +materialized: table
      +schema: mart